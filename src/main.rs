use rand::prelude::*;
use ndarray::{Array1, Array2};
use quizx::graph::{GraphLike, Graph};
use quizx::vec_graph::VecGraph;
use quizx::basic_rules::*;
use quizx::traits::*;
use quizx::graph::VertexType;
use quizx::graph::EdgeType;

fn main() {
    println!("asdfasdf");
}
